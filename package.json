{
  "name": "spice",
  "private": true,
  "version": "0.0.0",
  "description": "Spice — multi-platform music client (Next.js backend).",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "backend:dev": "pnpm --filter @spice/backend dev",
    "backend:build": "pnpm --filter @spice/backend build",
    "backend:build:local": "pnpm --filter @spice/backend build:local",
    "backend:build:vercel": "pnpm --filter @spice/backend build:vercel",
    "backend:lint": "pnpm --filter @spice/backend lint",
    "backend:typecheck": "pnpm --filter @spice/backend typecheck",
    "backend:package:local:windows": "pnpm --filter @spice/backend package:local:windows"
  }
}
